{
  "name": "game2",
  "version": "1.0.0",
  "description": "",
  "main": "dist/electron.js",
  "window": {
    "width": 800,
    "height": 600,
    "title": "My Game"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --config webpack.config.ts && npm run compile",
    "compile": "tsc src/Electron.ts --outDir dist && npm run rename",
    "rename": "node -e \"require('fs').rename('dist/Electron.js', 'dist/electron.js', () => {})\"",
    "start": "webpack-dev-server --open --watch",
    "electron": "electron .",
    "play": "npm run build && npm run electron"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/electron": "^1.6.10",
    "@typescript-eslint/parser": "^3.6.1",
    "electron": "^9.1.0",
    "eslint": "^7.4.0",
    "phaser": "^3.24.1",
    "ts-loader": "^8.0.1",
    "typescript": "^3.9.7",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.12",
    "webpack-dev-server": "^3.11.0"
  }
}
