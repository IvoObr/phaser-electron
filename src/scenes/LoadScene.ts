import Phaser from 'phaser';
import { ScreenSize, Scenes } from '../lib/consts';

export default class LoadScene extends Phaser.Scene {
    
    constructor() {
        super(Scenes.LoadScene);
    }

    preload(): void {
        
    }
    
    create(): void {

    }

    update(): void {

    }
}
