export enum eDirections {
    up = 'up',
    left = 'left',
    down = 'down',
    turn = 'turn',
    right = 'right',
    center = 'center'
}

export enum eObjs {
    ground = 'ground',
    
}

export enum eCharacters {
    
}
