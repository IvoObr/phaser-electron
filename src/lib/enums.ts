export enum directions {
    right = 'right',
    left = 'left',
    up = 'up',
    down = 'down',
    turn = 'turn',
    center = 'center'
}

export enum sceneObjs {
    ground = 'ground',
    // todo 
}
